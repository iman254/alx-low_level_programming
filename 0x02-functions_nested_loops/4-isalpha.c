#include "main.h"
/**
 * _isalpha - A function that checks for alphabetic character.
 *
 * @c: character being assessed.
 *
 * return: 0 or 1
 */
int _isalpha(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0)
}
