#include <stdio.h>
/**
 * main - Returns all singlgle digit numbers base 10starting from 0
 *
 * Return: Always 0
 */
int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}


